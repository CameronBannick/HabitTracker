import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cameronbannick.habittracker',
  appName: 'HabitTracker',
  webDir: 'dist'
};

export default config;
